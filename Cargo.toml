[package]
name = "stream"
version = "0.1.0"
edition = "2024"

[dependencies]
ariadne = { version = "0.5.0", features = ["auto-color"] }
chumsky = { version = "0.9.3", features = ["nightly", "std"] }
itertools = "0.13.0"
